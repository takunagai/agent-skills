---
type: 実行指示書
status: 未着手   # 未着手 → 実行中 → 完了（中止は「中止」）
作成日: <YYYY-MM-DD>
完了日:          # 完了時に実行セッションが記入
---

## 起動方法（人間が読む欄 — 実行セッションはこの章を読み飛ばし、「前提条件」から開始する）

- 作成日: <YYYY-MM-DD>
- 推奨モデル: <Opus / Sonnet>（理由: <◯◯の調査・判断が残るため 等>）
- 思考キーワード: <ultrathink（下の開始プロンプトに埋め込み済み） / なし>
- 起動コマンド（ターミナルで人間が実行）: claude --model <opus/sonnet> --permission-mode <acceptEdits / default>
- マニフェスト: <絶対パス/00_実行順.md（単発実行の指示書なら「なし」）>
- 開始プロンプト（コピペ用）:

  <絶対パス/この指示書.md> を読み、「起動方法」章は読み飛ばして「前提条件」から順に実行してください。
  開始時に <絶対パス/00_実行順.md> の本指示書の行を [/] に更新してください。
  前提条件を満たさない・判断に迷う・スコープ外の作業が必要になった場合は、
  作業を止めて質問してください（Stop And Ask）。
  完了したら「完了条件」の検証コマンドをすべて実行し、結果を実行ログ欄に記録し、
  本指示書の frontmatter を status: 完了・完了日記入に更新し、
  <絶対パス/00_実行順.md> の行を [x] と完了時刻・結果一行で更新してください。
  <ultrathink>

## 前提条件

- 対象ブランチ: <xxx>
- 未コミット変更の扱い: <クリーンであること / 許容範囲>
- 依存・サーバー起動要否: <なし / <手順>>
- 上記を満たさなければ Stop And Ask

## スコープ外

- <やらないことを明示。範囲外の実装はしない>

## Stop And Ask 条件

- <到達状態が不明・危険・スコープ外になったとき>
- <前提条件を満たさないとき>

## 最初に読むファイル

1. <path> — <なぜ読むか>

## 作業手順

1. <手順> ─ 到達状態: <検証可能な最終状態> / 検証: `<コマンド>`
2. <手順> ─ 到達状態: <検証可能な最終状態> / 検証: `<コマンド>`

## 完了条件（DoD）

- [ ] <lint / 型 / テスト / ビルド 等、機械判定できる検証>: `<コマンド>`
- [ ] <到達状態の確認>

## 失敗時の撤退

- ロールバック手順: <手順>
- リトライ可否: <可 / 不可（理由）>
- 人間への報告事項: <何をどう報告するか>

## 実行ログ欄（実行セッションが追記する）

-
